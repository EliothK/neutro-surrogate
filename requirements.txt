# Pinned versions of the direct dependencies, as used for the results in results/.
# pyproject.toml holds the looser minimum versions; this file is for reproducing results exactly.
# Install torch first if you need a specific CUDA build (see the README), for example:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu118
# then:
#   pip install -r requirements.txt
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
torch==2.5.1
matplotlib==3.11.1
optuna==5.0.0
pytest==9.1.1
